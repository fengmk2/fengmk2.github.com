name: Deploy
on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: voidzero-dev/setup-vp@42d342a4e04d7a57b70d0cdec88dc16a9d664315 # PR #54: respect project .npmrc
        with:
          cache: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
      - run: vp run sitemap
      - run: vpx void deploy
        env:
          VOID_TOKEN: ${{ secrets.VOID_TOKEN }}
